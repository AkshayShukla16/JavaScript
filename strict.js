
"use strict";

// Variable used to store some informaion
// We can use that info later
// we can also change that information

// variable declaration
bucket="Akshay";  // throws an error as we not declared bucket so var bucket ="Akshay" hona chahiye

// use a variable
console.log(bucket);

// changing the value of the variable
bucket="Ashish"; 

console.log(bucket);   // we can also use var here 