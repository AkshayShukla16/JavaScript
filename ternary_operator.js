// A ternary operator evaluates a condition and executes a block of code based on the condition.

// Its syntax is: condition ? expression1 : expression2
// A ternary operator can be used to replace an if..else statement in certain situations.
let age= 20;
let practise;
if(age>10){
    practise= "Ride bicycle";
}
else{
    practise="Play with toys";
}
console.log(practise);

// this is long process so here we use ternary operator

let sal = 3000;
let purchase= sal>2000 ? "You can buy cycle" : "You cannot buy cycle";
console.log(purchase);